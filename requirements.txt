# MAGI System IDE — inferencia de nube gratuita, sin claves de API,
# sin modelos locales (documento de arquitectura §I.3).
g4f
curl_cffi==0.5.10
httpx
requests==2.31.0
websockets==12.0
pydantic
numpy==1.24.4
scikit-learn==1.4.1.post1
pywebview==4.4.1
pyinstaller==6.21.0

# Ingeniería inversa y emuladores (§5.3). Ghidra y radare2 son opcionales:
# se detectan si están instalados y el toolchain funciona sin ellos.
capstone>=5.0
unicorn>=2.0

# Fábrica de artefactos con bucle de observación (§5). Opcionales: sin ellos
# el sistema genera igual, pero no puede MIRAR lo que produjo.
pygame
pillow
pypdf
python-docx

# Tests (CI)
pytest>=8.0
pytest-asyncio>=0.23
